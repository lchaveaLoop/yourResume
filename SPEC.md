# SPEC.md - yourResume 产品规格书

## 1. 项目概述

- **项目名称**: yourResume
- **类型**: 纯前端简历解析、编辑、预览与 PDF 导出工具
- **核心流程**: `pdf/docx/md 输入 → 浏览器本地解析 → 结构化编辑 → 模板预览 → PDF 导出`
- **当前原则**: 隐私优先、前端独立运行、先保证核心投递流程稳定。

---

## 2. 核心功能

| 功能 | 描述 | 状态 |
|------|------|------|
| 文件上传 | 支持 `.pdf` / `.docx` / `.md` / `.txt` 简历上传 | ✅ 已完成 |
| 简历解析 | 在浏览器内解析为统一 JSON 数据 | ✅ 已完成 |
| 字段编辑 | 支持基本信息、摘要、教育、经历、项目、技能编辑 | ✅ 已完成 |
| 照片上传 | 支持头像/照片以 base64 data URL 保存到当前简历数据 | ✅ 已完成 |
| 模板预览 | 支持 ATS、高级工程师、长简历、市场商务模板 | ✅ 已完成 |
| 模板推断 | 根据目标岗位和关键词自动选择职业模板 | ✅ 已完成 |
| PDF 导出 | 使用网页 DOM 渲染导出 A4 PDF，支持多页 | ✅ 已完成 |

---

## 3. 用户故事

### 场景 A：首次使用
用户上传一份已有 `.docx` 或 `.md` 简历，系统在浏览器内解析内容，生成可预览的网页版简历，并提供 PDF 下载。
也可以上传文本型 `.pdf` 简历；扫描件 PDF 暂不做 OCR，系统会提示用户换用文本型 PDF、DOCX 或 Markdown。

### 场景 B：编辑调整
用户在左侧编辑区修改姓名、联系方式、经历描述、项目描述或技能，右侧预览实时更新。

### 场景 C：模板切换
用户通过模板切换器选择 ATS、高级工程师、长简历或市场商务模板，预览和 PDF 导出保持一致。

### 场景 D：长简历导出
用户上传内容较长的简历，系统估算页数并允许导出多页 PDF。

---

## 4. 输入 / 输出定义

### 输入格式

#### Markdown

```md
# 姓名
- email@example.com
- 13800000000

## 求职意向
前端开发工程师

## 教育背景
- 学校 - 专业 · 学历 - 时间

## 工作经历
### 公司 - 职位 - 时间
- 负责事项

## 项目经历
### 项目 - 角色 - 时间
- 项目成果

## 技能
- Vue / TypeScript
```

#### docx

- 标准 Microsoft Word `.docx` 文件。
- 建议包含姓名、联系方式、求职意向、教育背景、工作经历、项目经历、技能等常见区块。
- 解析不要求固定模板，但复杂表格、图片化内容和非常规排版可能需要人工修正。

#### PDF

- 支持包含可复制文本的标准 `.pdf` 简历。
- PDF 文本提取在浏览器本地完成，不上传服务端。
- 扫描件或图片型 PDF 当前不做 OCR，无法提取文本时提示用户更换文本型 PDF、DOCX 或 Markdown。

### 中间数据结构

```json
{
  "name": "string",
  "email": "string",
  "phone": "string",
  "location": "string",
  "targetRole": "string",
  "summary": "string",
  "photo": "string",
  "education": [
    { "school": "string", "degree": "string", "duration": "string", "details": "string" }
  ],
  "experience": [
    { "company": "string", "title": "string", "duration": "string", "details": ["string"] }
  ],
  "skills": ["string"],
  "projects": [
    { "name": "string", "role": "string", "duration": "string", "details": ["string"] }
  ]
}
```

### 输出

- 网页预览：右侧 A4 简历预览区域。
- PDF 文件：默认文件名为 `{姓名}_简历.pdf`。

---

## 5. 边界情况处理

| 场景 | 处理方式 |
|------|----------|
| 上传非 `.pdf` / `.docx` / `.md` / `.txt` 文件 | 拒绝上传并提示支持格式 |
| 上传扫描件或图片型 PDF | 保持上传入口，提示 OCR 识别暂未支持 |
| 文件为空或解析失败 | 保持当前状态，提示用户检查文件内容 |
| 缺少核心字段 | 允许继续编辑和预览，缺失字段不渲染 |
| 内容超出一页 | 显示预计页数并导出多页 PDF |
| PDF 生成失败 | 显示失败提示，允许重试 |

---

## 6. 非功能需求

- **隐私**: 默认不上传用户简历，解析、预览和导出均在浏览器本地完成。
- **性能**: 常规简历解析目标 < 3s，预览更新目标 < 500ms。
- **兼容性**: Chrome / Edge / Safari 等现代浏览器。
- **可维护性**: 新模板应复用统一 `ResumeData` 数据结构，不引入独立数据模型。

---

*最后更新: 2026-05-15*
