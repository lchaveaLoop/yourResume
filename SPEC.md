# SPEC.md - yourResume 产品规格书

## 1. 项目概述

- **项目名称**: yourResume
- **类型**: AI Agent 类简历工具（Web 应用）
- **核心流程**: `docx/md 输入 → 解析为结构化数据 → 高颜值网页预览 → PDF 导出`
- **MVP 原则**: 先跑通核心流程，不过度工程化，渐进增强

---

## 2. 核心功能

### MVP 功能

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 文件上传 | 支持 .docx / .md 格式简历上传 | P0 |
| 简历解析 | 将上传文件解析为 JSON 结构化数据 | P0 |
| 网页预览 | 将解析结果以高颜值页面渲染展示 | P0 |
| PDF 导出 | 将网页版本导出为可投递 PDF | P0 |
| 基础编辑 | 在网页端对简历字段进行修改 | P1 |
| 多模板切换 | 提供 2~3 套预设模板风格 | P1 |

### 未来功能（渐进增强）

- AI 润色建议
- 一键优化排版
- 简历评分/关键词分析
- 多语言翻译
- 云端存储 & 历史版本

---

## 3. 用户故事

### 场景 A：首次使用
> 用户上传一份已有的 .docx 简历，系统在 3 秒内生成可预览的网页版，并提供 PDF 下载链接。

### 场景 B：编辑调整
> 用户对预览效果满意，但在某个项目经历上发现错别字，点击对应字段直接修改，网页实时更新。

### 场景 C：模板切换
> 用户觉得默认模板太正式，通过切换按钮换到"创意模板"，页面风格立即变更，PDF 同步更新。

### 场景 D：MD 用户
> 用户粘贴或上传 .md 格式的简历文本，系统解析为结构化内容，同样生成网页预览和 PDF。

---

## 4. 输入 / 输出定义

### 输入格式

#### docx
- 标准的微软 Word `.docx` 文件
- 建议包含：姓名、联系方式、教育背景、工作经历、技能证书等区块
- 解析时不强制要求特定模板，智能识别区块标题

#### Markdown
```
# 姓名
- 联系方式
## 教育背景
...
## 工作经历
...
## 技能
...
```

### 输出格式

#### JSON 结构（中间态）
```json
{
  "name": "string",
  "email": "string",
  "phone": "string",
  "location": "string",
  "summary": "string",
  "education": [
    { "school": "string", "degree": "string", "duration": "string", "details": "string" }
  ],
  "experience": [
    { "company": "string", "title": "string", "duration": "string", "details": "string[]" }
  ],
  "skills": ["string"],
  "projects": [
    { "name": "string", "role": "string", "duration": "string", "details": "string[]" }
  ]
}
```

#### 网页预览
- 响应式布局，支持 PC / 移动端预览
- 实时热更新（编辑后立即反映）

#### PDF 导出
- A4 幅面，标准简历尺寸
- 文件名：`{姓名}_简历.pdf`

---

## 5. 边界情况处理

| 场景 | 处理方式 |
|------|----------|
| 上传非 .docx/.md 文件 | 提示"仅支持 docx 和 md 格式"，拒绝上传 |
| 文件为空或解析失败 | 提示"无法识别简历内容，请检查格式" |
| 文件过大（>5MB） | 提示"文件过大，请控制在 5MB 以内" |
| 简历缺少核心字段 | 允许解析，仅缺失字段以空值展示，不阻断流程 |
| 网络错误（PDF 生成失败） | 提示错误，允许重试，不污染 UI |
| 移动端访问 | 提供基础可用性，核心功能不降级 |

---

## 6. 非功能需求

- **性能**: 简历解析 < 3s，网页渲染 < 500ms
- **安全**: 所有处理在本地/服务端完成，不外传用户简历数据
- **兼容性**: 现代浏览器（Chrome 90+, Edge 90+, Safari 14+）
- **无障碍**: 基础 ARIA 标签支持
